#!/usr/bin/env bash
set -euo pipefail

echo 'Auto formatting staged git changes...'

# ignore everything but staged changes inside of ./
if [[ -z $(git diff --name-only --cached ./) ]]; then
  exit 0
fi

make format-staged
